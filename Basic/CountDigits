number = int(input("Give your number"))

if number == 0:
    count = 1

else:
    count = 0
    while number > 0:
        count += 1
        number = number // 10

print(f"Count is {count}")
